<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Mi Perfil - TechPC Store</title>
  <link rel="stylesheet" href="/css/perfil.css" />
</head>
<body>
  <header>
    <h1>👤 Mi Perfil</h1>
    <a href="/home">⬅️ Volver al inicio</a>
  </header>

  <main class="perfil-container">
    <div class="perfil-card">
      <h2>Información del usuario</h2>

      <!-- Datos del usuario obtenidos desde el servidor -->
      <label>Nombre:</label>
      <input type="text" id="nombre" value="<%= usuario?.nombre || '' %>" readonly />

      <label>Apellido:</label>
      <input type="text" id="apellido" value="<%= usuario?.apellido || '' %>" readonly />

      <label>Correo:</label>
      <input type="email" id="correo" value="<%= usuario?.correo || '' %>" readonly />

      <label>Teléfono:</label>
      <input type="text" id="telefono" value="<%= usuario?.telefono || '' %>" readonly />

      <button id="editar-btn">Editar</button>
      <button id="guardar-btn" style="display:none;">Guardar cambios</button>
    </div>
  </main>

  <script src="/scripts/perfil.js"></script>
</body>
</html>
