<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title><%= titulo %></title>
  <link rel="stylesheet" href="/css/admin.css" />
</head>

<body>
  <header>
    <h1>Panel de Administración 👑</h1>

    <nav>
      <a href="/home">🏠 Volver al inicio</a>
      <a href="/auth/logout" id="logout-admin">🚪 Cerrar sesión</a>
    </nav>

    <% if (user) { %>
      <p class="bienvenida">
        Bienvenido, <strong><%= user.name %></strong>
        (<%= user.role %>)
      </p>
    <% } %>
  </header>

  <main>
    <!-- SECCIÓN USUARIOS -->
    <section id="seccion-usuarios">
      <h2>👤 Lista de usuarios registrados</h2>
      <table id="tablaUsuarios">
        <thead>
          <tr>
            <th>ID</th>
            <th>Nombre</th>
            <th>Apellido</th>
            <th>Correo</th>
            <th>Teléfono</th>
            <th>Rol</th>
            <th>Verificado</th>
            <th>Acciones</th>
          </tr>
        </thead>
        <tbody>
          <!-- Aquí se llenará la lista con JS -->
        </tbody>
      </table>
    </section>

    <hr />

    <!-- SECCIÓN PRODUCTOS -->
    <section id="seccion-productos">
      <h2>🛍️ Gestión de Productos</h2>

      <!-- Formulario para agregar producto -->
      <form id="form-nuevo-producto" enctype="multipart/form-data">
        <h3>➕ Agregar nuevo producto</h3>
        <div class="form-grid">
          <input type="text" name="nombre" placeholder="Nombre" required />
          <input type="text" name="descripcion" placeholder="Descripción" />
          <input type="number" name="precio" step="0.01" placeholder="Precio" required />
          <input type="number" name="stock" placeholder="Stock" required />
          <input type="file" name="imagen" accept="image/*" />
          <input type="text" name="categoria" placeholder="Categoría" />
        </div>
        <button type="submit" class="btn-agregar">Agregar producto</button>
      </form>
    </section>

    <!-- TABLA DE PRODUCTOS -->
    <table id="tablaProductos">
      <thead>
        <tr>
          <th>ID</th>
          <th>Nombre</th>
          <th>Descripción</th>
          <th>Precio</th>
          <th>Stock</th>
          <th>Categoría</th>
          <th>Imagen</th>
          <th>Acciones</th>
        </tr>
      </thead>
      <tbody>
        <!-- Productos se cargarán con JS -->
      </tbody>
    </table>

    <!-- FORMULARIO EDITAR PRODUCTO -->
    <div id="form-editar-producto-container" style="display: none;">
      <h3>✏️ Editar producto</h3>
      <form id="form-editar-producto" enctype="multipart/form-data">
        <input type="hidden" name="id" id="edit-id" />
        <div class="form-grid">
          <input type="text" name="nombre" id="edit-nombre" placeholder="Nombre" required />
          <input type="text" name="descripcion" id="edit-descripcion" placeholder="Descripción" />
          <input type="number" name="precio" id="edit-precio" step="0.01" placeholder="Precio" required />
          <input type="number" name="stock" id="edit-stock" placeholder="Stock" required />
          <input type="hidden" name="image_url_anterior" id="edit-image_url_anterior" />
          <input type="file" name="imagen" accept="image/*" />
          <input type="text" name="categoria" id="edit-categoria" placeholder="Categoría" />
        </div>
        <button type="submit" class="btn-editar">Guardar cambios</button>
        <button type="button" id="cancelar-edicion" class="btn-cancelar">Cancelar</button>
      </form>
    </div>
  </main>

  <footer>
    <p>© 2025 TechPC Store - Panel de Administración</p>
  </footer>

  <script src="/scripts/admin.js"></script>
</body>
</html>
